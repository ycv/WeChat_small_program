<view class="widgets__item" wx:for-items="{{array}}" wx:for-item="item">
    <navigator url="../video/video-av" class="widget">
        <view id="view" class="widgets__info" >
            <image class="widget__img" style="width: 100px; height: 100px; background-color: #eeeeee;" src="{{item.src}}" background-size="cover" ></image>
            <view class="widget__text">{{item.text}}</view>
        </view>
            <view class="widget__line"></view>
    </navigator>
</view>